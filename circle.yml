machine:
  pre:
    - sudo locale-gen fr_FR.UTF-8
  php:
    version: 5.6.5
  environment:
    LANG: fr_FR.UTF-8
  python:
    version: 3.4.3

dependencies:
  post:
    # Met-à-jour sqlite3 en 3.8
    - sudo apt-get install -t trusty-updates sqlite3
    - yes '' | pecl install PDO_SQLite imagick
    - pip install selenium libsass

test:
  pre:
    - env
  override:
    - make test
