<?php
$this->navigation($this->nav);
$this->navigation()
      ->sitemap()
      ->setFormatOutput(true); // default is false
$this->navigation()->sitemap()->setUseSitemapValidators(false);
echo $this->navigation()->sitemap();
